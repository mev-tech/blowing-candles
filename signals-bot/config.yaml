watchlist:
  - AAPL
  - MSFT
  - NVDA
  - TSLA

news:
  earnings_block_hours: 48
  ttl_minutes: 180
  local_earnings_calendar: earnings_calendar.json

policy:
  max_buys_per_day: 99999999999 # 2
  cooldown_minutes: 0 # 240

strategy:
  entry_mode: conservative   # opportunistic | balanced | conservative
  buy_high_score: 80     # HIGH confidence BUY threshold
  buy_low_score: 65      # LOW confidence BUY threshold (balanced/opportunistic)

output:
  text_file: signals.txt
  json_file: signals.json

state:
  path: data/state.json

audit:
  jsonl_path: logs/decisions.jsonl
